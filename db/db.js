/* const {
    config
} = require('process');

const knex = require('knex')({
    client: 'pg',
    connection: {
        host: '127.0.0.1',
        port: 3000,
        user: 'root',
        password: 'password',
        database: 'db'
    }
}); */